# Ejemplo de plantilla de variables de entorno.
# Este proyecto no requiere variables de entorno para funcionar (no hay backend real).
# Si en el futuro se integra un servicio externo (ej. pasarela de pago o API de stock),
# agregar aquí solo nombres de variables con valores genéricos, nunca credenciales reales.

# NEXT_PUBLIC_API_URL=https://api.ejemplo.com
# NEXT_PUBLIC_SITE_NAME=Tienda de Neumáticos
